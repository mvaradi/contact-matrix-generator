language: python
python:
    - 3.9
install:
  - pip install codecov
  - pip install pytest-cov
  - pip install -r requirements.txt
script:
  - pytest tests --cov=funpdbe_client
after_success:
  - codecov
